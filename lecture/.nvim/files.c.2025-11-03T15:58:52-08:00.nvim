#include <fcntl.h>
#include <stdio.h>
#include <unistd.h>

int main(void) {
  int file = open("tmp", O_CREAT | O_RDWR, S_IUSR | S_IWUSR);
  write(file, "Hello World!", sizeof("Hello World"));
  close(file);
}
