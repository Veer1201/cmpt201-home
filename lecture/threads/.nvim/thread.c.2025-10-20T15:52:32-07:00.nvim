#include <pthread.h>
#include <stdio.h>

void *thread_func(void* arg) {
  printf("Hii\n");
  return NULL;
}

int main(void) {
  pthread_t thread;

  pthread_create(&thread, NULL, thread_func, NULL);

  pthread_join(thread, NULL);
  return 0;
}
